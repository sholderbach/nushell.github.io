import{_ as n,M as s,p as t,q as d,Q as r,t as e,N as c,U as l,a1 as a}from"./framework-344bb0e4.js";const i={},u=a(`<h1 id="el-pipeline" tabindex="-1"><a class="header-anchor" href="#el-pipeline" aria-hidden="true">#</a> El pipeline</h1><p>Uno de los diseños centrales de Nu es la tubería (pipeline), una idea de diseño que rastrea sus raíces décadas atrás hasta parte de la filosofía original detrás de Unix. Así como Nu extiende desde un solo dato tipo de cadena de Unix, Nu también extiende la idea de tuberías (pipelines) para incluir más que solo texto.</p><h2 id="fundamentos" tabindex="-1"><a class="header-anchor" href="#fundamentos" aria-hidden="true">#</a> Fundamentos</h2><p>Una tubería (pipeline) es construída con tres partes: la entrada, el filtro, y la salida.</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; open &quot;Cargo.toml&quot; | inc package.version | save &quot;Cargo_new.toml&quot;
</code></pre></div><p>El primer comando, <code>open &quot;Cargo.toml&quot;</code>, es una entrada (a veces también llamada &quot;fuente&quot; o &quot;productor&quot;). Esto crea o carga datos y lo canaliza en la tubería. Es de entrada para la tubería tener valores y poder trabajarlas. Comandos como <code>ls</code> también son entradas ya que toman datos desde el sistema de archivos y lo canalizan a través de las tuberías para que puedan ser usadas.</p><p>El segundo comando, <code>inc package.version</code>, es un filtro. Filtros toman los datos que se les entrega y comúnmente hacen algo con la misma. Puede que la cambien (tal como el comando <code>inc</code> en nuestro ejemplo), o pueden hacer otra operación, como registrar, mientras pasan los valores.</p><p>El último comando, <code>save &quot;Cargo_new.toml&quot;</code>, es una salida (a veces llamado un &quot;sink&quot;). Una salida toma la entrada de la tubería y realiza alguna operación final. En nuestro ejemplo, grabamos lo que viene a través de la tubería hacia un archivo como paso final. Otros tipos de comandos de salida pueda que tomen valores y lo muestren para el usuario.</p><h2 id="trabajando-con-comandos-externos" tabindex="-1"><a class="header-anchor" href="#trabajando-con-comandos-externos" aria-hidden="true">#</a> Trabajando con comandos externos</h2>`,9),m=a(`<p><code>commando_interno | comando_externo</code></p><p>Los datos fluirán desde comando_interno al comando_externo. Estos datos se esperan que sean cadenas para que puedan ser enviadas a entrada estándar <code>stdin</code> del comando_externo.</p><p><code>comando_externo | comando_interno</code></p><p>Los datos que vienen de un comando externo a Nu se coleccionará a una sola cadena y luego serán pasadas a comando_interno. Comandos como <code>lines</code> ayudan a facilitar datos provenientes de comandos externos de manera que sea más facil trabajarlas.</p><p><code>comando_externo_1 | comando_externo_2</code></p><p>Nu trabaja con datos canalizados entre dos comandos externos de la misma forma como en otras shells, como Bash lo haría. La salida estándar <code>stdout</code> de comando_externo_1 es conectada a la entrada estándar <code>stdin</code> de comando_externo_2. Esto permite que los datos fluyan naturalmente entre dos comandos.</p><h2 id="detras-de-escenas" tabindex="-1"><a class="header-anchor" href="#detras-de-escenas" aria-hidden="true">#</a> Detrás de escenas</h2><p>Es posible que te hayas preguntado como miramos una tabla si <code>ls</code> es una entrada y no una salida. Nu agrega esta salida por nosotros automáticamente usando otro comando llamado <code>autoview</code>. El comando <code>autoview</code> es añadido a cualquier tubería que no tenga una salida permitiéndonos ver el resultado.</p><p>En efecto, el comando:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ls
</code></pre></div><p>y la tubería:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ls | autoview
</code></pre></div><p>Son uno y lo mismo.</p>`,13);function p(h,x){const o=s("RouterLink");return t(),d("div",null,[u,r("p",null,[e("Los comandos en Nu se comunican entre ellos usando los tipos de datos de Nu (mirar "),c(o,{to:"/es/book/tipos_de_datos.html"},{default:l(()=>[e("tipos de datos")]),_:1}),e(") pero, ¿qué hay de comandos fuera de Nu? Miremos algunos ejemplos trabajando con comandos externos:")]),m])}const g=n(i,[["render",p],["__file","pipeline.html.vue"]]);export{g as default};
