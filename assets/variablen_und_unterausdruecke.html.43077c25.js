import{_ as e,z as n,A as a,a6 as r}from"./framework.3d018c9f.js";const d={},t=r(`<h1 id="variablen-und-unterausdrucke" tabindex="-1"><a class="header-anchor" href="#variablen-und-unterausdrucke" aria-hidden="true">#</a> Variablen und Unterausdrücke</h1><p>Es gibt zwei Arten von auszuwertenden Ausdrücken in Nushell: Variablen und Unterausdrücke. Diese werden durch das Dollar Symbol (<code>$</code>) eingeleitet. Das gibt den Hinweis, dass Nushell an dieser Stelle einen Ausdruck auswerten muss, um einen Wert zu erhalten, der für die Ausführung eines gesamten Befehls benötigt wird. Beide Formen des Ausdrucks haben eine einfache Variante und eine &#39;Pfad&#39;-Variante, wenn mit komplexeren Daten gearbeitet wird.</p><h2 id="variablen" tabindex="-1"><a class="header-anchor" href="#variablen" aria-hidden="true">#</a> Variablen</h2><p>Die einfachere Variante der auszuwertenden Ausdrücke ist die Variable. Während der Auswertung, wird die Variable durch ihren Wert ersetzt.</p><p>Wenn eine Variable erzeugt wurde, kann der Inhalt dieser Variable ausgegeben werden, indem <code>$</code> vor dem Variablennamen verwendet wird:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; let my_value = 4
&gt; echo $my_value
4
</code></pre></div><h2 id="pfade-von-variablen" tabindex="-1"><a class="header-anchor" href="#pfade-von-variablen" aria-hidden="true">#</a> Pfade von Variablen</h2><p>Ein Pfad einer Variable funktioniert ähnlich wie ein strukturierter Datentyp. Es kann mittels Referenzen auf den Inhalt der Variable beziehungsweise die Spalten in der Variable zugegriffen werden, um final bei einem bestimmten Wert zu landen. Wenn beispielsweise anstatt der <code>4</code> im obigen Beispiel, der Variablen eine Tabelle zugewiesen wurde:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; let my_value = [[name]; [testuser]]
</code></pre></div><p>Hier kann ein Pfad der Variable <code>$my_value</code> verwendet werden, um den Wert der Spalte <code>name</code> in nur einem Schritt zu bekommen:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; echo $my_value.name
testuser
</code></pre></div><h2 id="unterausdrucke" tabindex="-1"><a class="header-anchor" href="#unterausdrucke" aria-hidden="true">#</a> Unterausdrücke</h2><p>Unterausdrücke können jederzeit ausgewertet und die Ergebnisse verwendet werden, indem der Ausdruck in Klammern <code>()</code> gepackt wird. Hinweis: In älteren Versionen von Nushell (älter als 0.32) wurde <code>$()</code> benutzt.</p><p>Die Klammern enthalten eine Pipeline, die bis zum Ende durchlaufen wird und deren Ergebnis dann verwendet wird. Hier einige Beispiele: <code>(ls)</code> würde den <code>ls</code> Befehl ausführen und die entsprechende Tabelle zurückgeben und <code>(git branch --show-current)</code> führt den externen Befehl <code>git</code> aus und gibt einen String mit dem Namen des aktuellen Branch zurück. Diese Klammern können auch verwendet werden, um mathematische Ausdrücke, wie <code>(2 + 3)</code>, auszuwerten.</p><p>Unterausdrücke können auch ganze Pipelines statt nur einzelner Befehle enthalten. Um eine Liste von Dateien mit einer Größe größer als 10 Kilobytes zu bekommen, kann die folgende Pipeline verwendet und einer Variable zugewiesen werden:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; let names_of_big_files = (ls | where size &gt; 10kb)
&gt; echo $names_of_big_files
───┬────────────┬──────┬──────────┬──────────────
 # │    name    │ type │   size   │   modified
───┼────────────┼──────┼──────────┼──────────────
 0 │ Cargo.lock │ File │ 155.3 KB │ 17 hours ago
 1 │ README.md  │ File │  15.9 KB │ 17 hours ago
───┴────────────┴──────┴──────────┴──────────────
</code></pre></div><h2 id="unterausdrucke-und-pfade" tabindex="-1"><a class="header-anchor" href="#unterausdrucke-und-pfade" aria-hidden="true">#</a> Unterausdrücke und Pfade</h2><p>Unterausdrücke unterstützen auch Pfade. Um beispielsweise eine Liste der Dateinamen im aktuellen Ordner zu bekommen, kann diese Pipeline verwendet werden:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ls | get name
</code></pre></div><p>Dasselbe Ergebnis kann auch in nur einem Schritt erreicht werden, indem ein Unterausdruck mit Pfad verwendet wird:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; echo (ls).name
</code></pre></div><p>Welcher Stil gewählt wird, hängt vom Anwendungsfall und den persönlichen Vorlieben ab.</p><h2 id="short-hand-unterausdrucke" tabindex="-1"><a class="header-anchor" href="#short-hand-unterausdrucke" aria-hidden="true">#</a> Short-hand Unterausdrücke</h2><p>Nushell erlaubt den Zugriff auf Spalten in Tabellen in Unterausdrücken durch einfache Short-Hands. Wenn beispielsweise nur Zeilen in <code>ls</code> angezeigt werden sollen, in der die Größe der Datei größer als 10 Kilobytes ist, kann der folgende Befehl verwendet werden:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ls | where size &gt; 10kb
</code></pre></div><p><code>where size &gt; 10kb</code> ist ein Befehl mit zwei Teilen: Dem Befehlsnamen <code>where</code> und dem short-hand Ausdruck <code>size &gt; 10kb</code>. Hier wird auch klar, warum das Ganze short-hand heißt: <code>size</code> ist hier die gekürzte Version von <code>$it.size</code>. Das Ganze könnte auch mit einer der folgenden ausführlicheren Varianten erreicht werden:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ls | where $it.size &gt; 10kb
&gt; ls | where ($it.size &gt; 10kb)
&gt; ls | where {|$it| $it.size &gt; 10kb }
</code></pre></div><p>Damit diese short-hand Syntax funktioniert, muss der Name der Spalte auf der linken Seite der Operation sein (wie bei <code>size</code> in <code>size &gt; 10kb</code>).</p>`,28),i=[t];function s(u,l){return n(),a("div",null,i)}const o=e(d,[["render",s],["__file","variablen_und_unterausdruecke.html.vue"]]);export{o as default};
